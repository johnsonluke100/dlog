version: "3.9"

services:
  dlog-api:
    build: .
    image: dlog-api:latest
    restart: unless-stopped
    ports:
      - "8888:8888"
    environment:
      - RUST_LOG=info
      - OMEGA_ROOT=/data
    volumes:
      - ./data:/data
