[workspace]
members = [
    "spec",
    "corelib",
    "core",
    "api",
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
