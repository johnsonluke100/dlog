[package]
name = "core"
version = "0.1.0"
edition = "2021"
description = "DLOG Ω core CLI (simulation / tick runner placeholder)"

[dependencies]
spec = { path = "../spec" }
corelib = { path = "../corelib" }
