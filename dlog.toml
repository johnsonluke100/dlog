# dlog.toml
# Basic configuration for the local node.

# Human-readable name for this node.
node_name = "dlog-node-local"

# Bind address for the HTTP API.
bind_addr = "0.0.0.0:8080"

# Public URL (optional). Leave empty or comment out if not needed.
# public_url = "https://example.com"

# Conceptual phi tick rate (Ω ticks per second).
# Think of this as "server physics heartbeat". Clients with different FPS
# will ask /ticks/tune or /mc/register how to map this to their frames.
phi_tick_rate = 1000.0
