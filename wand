#!/usr/bin/env bash
set -euo pipefail

# Always run relative to this file's directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Hand everything off to refold.command wand (which will summon the vortex/key as needed)
./refold.command wand "$@"
