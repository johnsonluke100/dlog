[package]
name = "corelib"
version = "0.1.0"
edition = "2021"
description = "Core DLOG Ω state machine helpers"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
spec = { path = "../spec" }
